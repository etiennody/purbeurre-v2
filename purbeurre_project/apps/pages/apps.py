"""Pages apps
"""
from django.apps import AppConfig


class PagesConfig(AppConfig):
    """Pages configuration app named pages

    Args:
        AppConfig (subclass): instance for pages installed application
    """
    name = 'pages'
